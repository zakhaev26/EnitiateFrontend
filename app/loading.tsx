// Import necessary modules
"use client"
import { AnimatePresence } from 'framer-motion';

// Functional component for displaying loading animation
const Loading = () => {
    return (
        // AnimatePresence component from framer-motion for managing enter/exit animations
        <AnimatePresence />
    )
}

// Export the Loading component
export default Loading;
